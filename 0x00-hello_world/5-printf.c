#include <stdio.h>

/**
*main - prints the string in the printf function
*
*Description: Using the printf function
*This program prints "a C program that prints exactly with proper grammar, but the outcome
*is a piece of art"
* Return (0)
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
